@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome to MusicWebApp</h1>
    <p>Click on the buttons below to view Songs and Albums.</p>

    <!-- Botón para ir a la vista de Canciones -->
    <a href="@Url.Action("Index", "Songs")" class="btn btn-primary btn-lg">View Songs</a>

    <!-- Botón para ir a la vista de Álbumes -->
    <a href="@Url.Action("Index", "Albums")" class="btn btn-secondary btn-lg">View Albums</a>
</div>
